<div class="container">

  <div class="row">
    <div class="col-lg-5">
      <img class=" img-thumbnail profilePic" src="<%= user.profileImageSRC %>" alt="<%= user.username %>"><br>
      <a href="/user/images/new" class="btn btn-outline-primary">Add image</a>
    </div>
    <div class="col-lg-1"></div>
    <div class="col-lg-5 gallery">
      <h1 class="col-lg-12"><%= user.username %></h1>
      <ul class="imagesList">
      <% user.images.forEach((image) => { %>
        <div class="row">
        <li class="col-lg-6"><img class="img-thumbnail" thumbnail src="<%= image.src %>"></li>
        <li class="col-lg-6"><%= image.caption %></li>
        </div>
        <% }) %>
      </ul>
    </div>
  </div>
</div>
